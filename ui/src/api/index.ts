import * as auth from './auth';
import * as item from './item';
import * as product from './product';
import * as store from './store';
import * as user from './user';

export const api = { auth, item, product, store, user };
